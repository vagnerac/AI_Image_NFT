import dotenv from 'dotenv';
dotenv.config();

// app class to process main functions from the system
export class App {
  constructor() {}

  async runApp() {}
}

const app = new App();
app.runApp();
